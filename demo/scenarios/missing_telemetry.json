{
  "id": "missing_telemetry",
  "title": "Missing telemetry",
  "description": "The telemetry feed disconnects. The system must show 'Telemetry unavailable — last update Xs ago' and MUST NOT display fake live values. A TELEMETRY_STALE event is pushed over the WebSocket.",
  "machine_id": "EXC001",
  "operator_id": "OP1001",
  "steps": [
    { "t_seconds": 0, "inject": { "machine_state": "OPERATING" } },
    { "t_seconds": 5, "stop_feed": true },
    { "t_seconds": 20, "expect": { "ws_event": "TELEMETRY_STALE", "stale": true,
        "ui": "Telemetry unavailable — last update ~15s ago", "fake_live_data": false } }
  ],
  "provenance": "SIMULATED"
}
