# Demo

Owner: Claude 4. Deterministic demo scenarios that drive the simulator. See
`../docs/DEMO.md` for the 5–7 minute story and the scenario table. Each file in
`scenarios/` is a declarative timeline with `inject` steps, `operator_action`
steps, and `expect` assertions so the demo doubles as an end-to-end check.
